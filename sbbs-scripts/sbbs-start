#!/bin/bash

set -e
NAME="Synchronet BBS"

export PATH=$PATH:${SBBSEXEC}

# Change to scripts directory
cd -P -- "$(dirname -- "$0")"

# Run initialization
node -r esm init.js

cd ${SBBSCTRL}
exec /sbbs/exec/sbbs
